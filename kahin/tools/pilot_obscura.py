"""pilot_obscura.py — Obscura (Chrome/CDP) specific PILOT tools.

Task 3 fills these in: Obscura-only browsing capabilities that cannot be
expressed through the engine-agnostic PILOT layer (e.g. CDP-specific page
management and raw protocol access beyond kahin_execute_cdp).
"""

# Registration stubs (filled in Task 3):
#
# from kahin.oracle import mcp
# from kahin.tools._common import _DW, _RO, _RW
#
# @mcp.tool(name="kahin_...", annotations=_RO)
# async def _() -> str:
#     """..."""