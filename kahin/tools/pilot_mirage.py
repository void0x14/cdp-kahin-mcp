"""pilot_mirage.py — Mirage (Camoufox/Juggler) specific PILOT tools.

Task 3 fills these in: Mirage-only Juggler capabilities (DOM, Input, PageEx
tools) that surface real Juggler methods through Mirage.call().
"""

# Registration stubs (filled in Task 3):
#
# from kahin.oracle import mcp
# from kahin.tools._common import _DW, _RO, _RW
#
# @mcp.tool(name="kahin_...", annotations=_RO)
# async def _() -> str:
#     """..."""